/* ============================================================================
   ACCOUNT / ADAPTERS / INSTALLED — the providers this build registers.

   Development stand-ins, both labelled on screen. A production identity
   provider and a backend customer API replace these two lines; the screens
   and the session model do not change.
   ========================================================================= */

import './dev-auth.js';
import './dev-customer.js';
