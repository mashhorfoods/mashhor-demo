/* ============================================================================
   BOOKING / ADAPTERS / INSTALLED — the supplier adapters this build ships.

   Importing an adapter module registers it (see index.js). A real supplier
   goes here as one more line; the development adapter stays until then and
   is removed, not hidden, when it is replaced.
   ========================================================================= */

import './dev-flights.js';   // fictional carriers, clearly labelled — never real inventory
