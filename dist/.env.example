# عون الدرب للنقل المتخصص — backend configuration
#
# COPY THIS FILE TO .env AND FILL IT IN. NEVER COMMIT .env.
#
# Preferred location on Hostinger: one level ABOVE public_html, i.e.
#   /home/<user>/.env
# The application looks there first. If it is not found, it falls back to
#   public_html/app/.env
# which app/.htaccess denies to the web — but above the web root is stronger,
# because a misconfigured host cannot serve what is not under the document
# root at all. See DEPLOY.md.

# --- application -------------------------------------------------------------
APP_ENV=production          # production | development
APP_DEBUG=false             # true only on a machine nobody else can reach
APP_URL=https://aunaldrb.com

# --- database ----------------------------------------------------------------
# On Hostinger: hPanel → Databases → MySQL Databases. The host is almost always
# localhost; the database name and user both carry the account prefix.
DB_DRIVER=mysql             # mysql | sqlite  (sqlite is for local development)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=
DB_USER=
DB_PASS=
DB_CHARSET=utf8mb4

# sqlite only — path is relative to the project root
DB_SQLITE_PATH=app/storage/aun.sqlite

# --- sessions ----------------------------------------------------------------
# APP_KEY signs nothing on its own but seeds token derivation. Generate with:
#   php -r "echo bin2hex(random_bytes(32));"
APP_KEY=
SESSION_COOKIE=aun_sid
SESSION_IDLE_MINUTES=120        # inactivity before a session is rejected
SESSION_ABSOLUTE_HOURS=12       # hard ceiling regardless of activity
SESSION_COOKIE_SECURE=true      # false ONLY for local http development

# --- public request endpoint -------------------------------------------------
REQUESTS_RATE_PER_HOUR=8        # submissions allowed per IP per hour
REQUESTS_DEDUPE_MINUTES=10      # window in which an identical submission is
                                # treated as a retry rather than a new request

# --- one-time installation ---------------------------------------------------
# Set this ONLY while running install.php, and delete the line afterwards.
# Without it install.php answers 404 to everyone, including you. Generate with:
#   php -r "echo bin2hex(random_bytes(16));"
# Must be at least 16 characters. install.php refuses to run once a Super Admin
# exists, and removes itself after a successful install.
#SETUP_TOKEN=

# --- استعادة الدخول -----------------------------------------------------------
# RECOVERY_TOKEN turns on /recover.php — the way back in when nobody can sign
# in: the only Super Admin forgot the password, or locked the account out with
# failed attempts, and this hosting plan has no terminal to run a script from.
#
# Leave it commented out. Without it /recover.php answers 404 to everything, so
# it can ship on the server and still be closed. Set it only while you are
# recovering, then delete the line again — bin/preflight.php reports it as an
# open door for as long as it is set.
#
# Generate one (at least 24 characters):
#   php -r "echo bin2hex(random_bytes(16));"
#
# The page never displays a password: it lets you type a new one for a Super
# Admin, clear a lock, re-enable an account, and — only when no Super Admin is
# left at all — create one. Everything it does is written to the activity log.
#RECOVERY_TOKEN=

# --- كم تُحفظ السجلات ---------------------------------------------------------
# Two tables grow with use: activity_log (one row per administrative action)
# and content_publishes (one row per publish, of which only the newest is ever
# read). Left alone they fill the disk slowly and make every backup heavier.
#
# The sweep runs at most once a day on sign-in, and records its own deletions
# in the activity log, so a gap there is never unexplained. `php bin/prune.php`
# shows what the two tables hold; `--run` sweeps now.
#
# How long an activity entry is kept. Default 365 — long enough to settle any
# dispute about a request or a content change. Values under 30 are refused.
#ACTIVITY_KEEP_DAYS=365
#
# The emergency brake: a hard row cap, so a runaway that logs on every request
# is bounded in hours rather than in days. The newest rows are the ones kept.
#ACTIVITY_MAX_ROWS=50000
#
# How many publish records to keep. Nothing reads past the newest one, so this
# is history for a person to look at, not data the system needs.
#PUBLISH_KEEP_ROWS=200
#
# Notifications — the bell. A notification is a nudge about a request, not a
# record of one: the request stays in the system either way, so these age out
# faster and their removal is not written to the activity log. The bell reads
# at most 200, so NOTIFICATION_MAX_ROWS is never allowed below that.
#NOTIFICATION_KEEP_DAYS=90
#NOTIFICATION_MAX_ROWS=500
